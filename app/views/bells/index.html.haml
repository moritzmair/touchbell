%p#notice= notice
%h1 Bells

= link_to 'Neue Klingel anlegen', new_bell_path, class: 'btn btn-success'
%br
%br
%table
  %thead
    %tr
      %th Name
      %th Trigger
      %th Logo
      %th{:colspan => "3"}
  %tbody
    - @bells.each do |bell|
      %tr
        %td= bell.name
        %td= bell.trigger
        %td= image_tag(bell.logo+'-/resize/x60/')
        %td= link_to 'Anzeigen', bell, class: 'btn btn-light'
        %td= link_to 'Bearbeiten', edit_bell_path(bell), class: 'btn btn-primary'
        %td= link_to 'Löschen', bell, method: :delete, data: { confirm: 'Bist du sicher?' }, class: 'btn btn-danger'
